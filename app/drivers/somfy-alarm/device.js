'use strict';

// Somfy Protect alarm. All behaviour lives in lib/somfy-alarm-device.js —
// there is only one model here, so unlike the Imou cameras there is no
// per-model profile to name.

const SomfyAlarmDevice = require('../../lib/somfy-alarm-device');

module.exports = SomfyAlarmDevice;
