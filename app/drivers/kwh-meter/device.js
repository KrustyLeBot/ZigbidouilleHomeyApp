'use strict';

// All behaviour lives in lib/kwh-meter-device.js — there is only one model
// here, the virtual meter itself, so there is nothing driver-specific to add.

const KwhMeterDevice = require('../../lib/kwh-meter-device');

module.exports = KwhMeterDevice;
